<div class="jumbotron">
    <h1 class="display-3">Welcome</h1>
    <p class="lead">This is your dashboard, {{#if user}}{{user.name}}{{/if}}.</p>
    <span style="padding: 10px;"><a class="regbtn btn btn-primary btn-sm" href="#">Add a Contact</a></span>
</div>
<div class="container">
    <div class="row space">
        <div class="col-md-12">
        	Nothing to display.
        </div>
    </div>
</div>